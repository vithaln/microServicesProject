package com.example.demo;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class UserServiceApplicationTests {

	/*
	 * @Autowired private RatingService ratingService;
	 */
	@Test
	void contextLoads() {
	}

	/*
	 * @Test void createRatings() {
	 * 
	 * Rating rating = Rating.builder().rating(10).userId("").hotelId("").
	 * feedback("rating created by using feignClient").build(); Rating savedRatings
	 * = ratingService.createRatings(rating);
	 * 
	 * System.out.println("===created ratings by using feignClient...===="); }
	 */
}
